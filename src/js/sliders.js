import { tns } from '../../node_modules/tiny-slider/src/tiny-slider'
tns({
  container: '.compare-slider_1',
  items: 1,
  navPosition: 'bottom',
  controlsContainer: '.compare-slider_1-controls',
  lazyload: true,
})
tns({
  container: '.compare-slider_2',
  items: 1,
  navPosition: 'bottom',
  controlsContainer: '.compare-slider_2-controls',
  lazyload: true,
})
tns({
  container: '.directions-slider',
  items: 1,
  navPosition: 'bottom',
  controlsContainer: '.directions-slider-controls',
  lazyload: true,
})
